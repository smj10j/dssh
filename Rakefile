require "bundler/gem_tasks"

desc "Instructions"
task :default do
  puts "usage: rake clean"
  puts "run `rake -T` for more tasks"
end

desc "Clean up our previous mess."
task :clean do
  sh "rm -f dssh*gem"
end


